# 项目经历优化稿（黑马点评 · 精简版）

> 原文 324 字 → 优化稿 393 字（+21%，符合"最多增加三分之一"要求）

## 优化后正文（可直接替换原段落）

用户登录鉴权：使用ThreadLocal保存用户信息，登录成功后写入线程本地变量，退出登录后移除，配合双层拦截器实现token 30分钟滑动续期。缓存治理：使用Redis缓存用户及商铺信息，通过返回空值（2分钟TTL）解决缓存穿透，TTL叠加60~300秒随机值防止缓存雪崩，使用互斥锁解决缓存击穿，另实现逻辑过期+异步重建方案可切换。秒杀与高并发处理：基于Redis+Lua脚本将库存校验、原子扣减、一人一单校验封装为一次原子操作，解决超卖与重复下单，应用层Redisson分布式锁、DB乐观锁构成三层防线。订单写入Redis Stream消费组异步落库，手动ACK与pending-list兜底，提升系统吞吐。实现用户关注、点赞、签到功能，使用Redis Set存储关注关系并求交集实现共同关注，使用Sorted-Set有序集合实现点赞排行榜，使用BitMap位图实现用户签到统计。

---

## 相比原文新增的干货（均为代码中真实实现）

1. **双层拦截器 + token 30分钟滑动续期**（原文只写 ThreadLocal）
2. **空值 TTL 精确为 2 分钟**（原文没写时长）
3. **TTL 随机抖动 60~300 秒**（原文只说"不同TTL"）
4. **逻辑过期 + 异步重建备用方案**（代码中已实现，体现方案对比）
5. **三层防线**：Lua 原子操作 + Redisson 锁 + DB 乐观锁（原文只有 Redisson）
6. **Redis Stream 消费组 + 手动 ACK + pending-list 兜底**（原文只说"消息队列"）
7. **共同关注交集**（原文只写 Set 存关注）

## 说明

- 为控制字数，"关注流推送/滚动分页、RedisIdWorker、库存预热、Docker 部署"等亮点未写入；如面试官问到可口头补充。
- 全文技术点已与仓库代码逐行核对，无虚构内容，未编造压测数据。
