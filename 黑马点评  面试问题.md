# 黑马点评  面试问题

黑马点评 面试问题模拟

**一、项目整体设计**

1\.你这个项目的整体架构是怎样的？

项目整体是**单节点分层架构 \+ Redis 高性能能力增强 \+ 异步秒杀链路**，是经典的controller\+service\+mapper，基础设施有redis（缓存、登录态、ZSet/Set/Bitmap/GEO、Redis Stream），redisson（分布式锁（秒杀消费端按用户加锁））和拦截器（token 刷新 \+ 登录鉴权，ThreadLocal 保存用户上下文）。

2\.为什么选择 Redis ？解决什么问题？

- **Redis（主链路）解决的是“高并发实时处理”问题**

    - 商品秒杀用 `Redis + Lua脚本`，把库存校验、扣减、一人一单放在一个原子脚本里，先在缓存层快速拦截无效请求。校验通过先扣减缓存商品数量，异步开启线程进行创建订单数据入库。

    - 这样能把大量请求挡在 DB 前面，核心收益是**防超卖、快速失败、削峰降压**。

    - 同时 Redis 还承担缓存、登录态、点赞/关注/签到/GEO 等高频读写场景，降低数据库压力。

Redis Stream 是什么？

它本质是：

**一种消息队列（Message Queue）数据结构**
用来做 **系统解耦 \+ 异步处理 \+ 削峰填谷 \+ 最终一致性**
Redis Stream 只是 **存放消息 \+ 传递消息** 的中间媒介。

用户下单 → 主业务完成 → 写入 Redis Stream → 返回成功

后台线程 → 从 Stream 读取消息 → 处理订单后续逻辑

3\.秒杀系统的核心难点有哪些？

- **瞬时高并发冲击**

    - 大量请求在短时间涌入，DB 扛不住。

    - 做法：入口前置到 `Redis + Lua` 原子校验，失败快速返回，把无效请求挡在最前面。

- **超卖与重复下单**

    - 高并发下最容易出现库存被扣成负数、同一用户多单。

    - 做法：Lua 内做“库存校验 \+ 一人一单”；消费端再用 `Redisson` 用户维度锁 \+ DB 条件扣减（`stock > 0`）双保险。

- **异步链路一致性与可靠性**

    - 异步后会有消息丢失、重复消费、消费异常等问题。

    - 做法：主链路用 `Redis Stream`，消费者异常可以通过 pending\-list 重试；订单侧做幂等校验防止重复消费带来的错误，保证最终一致。

- **缓存与数据库一致性**

- 采用“更新 DB \+ 删除缓存”策略

4\.你认为你这个项目里最有技术含量的部分是哪一块？

- **入口原子校验（Redis \+ Lua）**

    - 在脚本里一次性完成库存判断、扣减、一人一单判断，并把合格请求写入消息流。

    - 价值是把高并发请求先在 Redis 层快速分流，减少 DB 压力。

- **异步解耦（Redis Stream）**

    - 请求线程不直接落库，改为异步消费创建订单，实现削峰填谷。

    - 消费异常时处理 pending\-list，避免消息处理“悬空”。

- **并发一致性兜底（Redisson \+ DB 条件更新）**

    - 消费端按用户加分布式锁，避免同用户并发重复下单；

    - 扣库存用 `stock > 0` 条件更新防超卖，和前面的 Lua 形成双重保障。

5\.如果用户量扩大 十倍百倍，你的系统瓶颈在哪里？

- **第一瓶颈：单体应用实例 \+ 本地线程消费模型**

    - 现在秒杀消费是应用内单线程处理（`VoucherOrderServiceImpl` 里的单线程执行器），吞吐上限会先到。

    - 放大后会出现消息堆积、下单延迟上升。

    - 解决办法：引入RocketMQ

- **第二瓶颈：Redis 单点压力（热点 Key/大流量读写）**

    - 登录态、缓存、Lua、Stream、社交结构都在 Redis，热点时期 CPU 和网络都会被打满。

    - 如果是单机 Redis，风险更高。

    - 解决办法：主从\+哨兵/集群，热点 key 分片，本地缓存 \+ 多级缓存。

**二、用户认证与 ThreadLocal**

1\.用户认证和鉴权的完整链路是怎么流转和实现的？

- 完整流程：

    - 用户首先在登录页面输入手机号，请求发送验证码接口，后端会校验手机号是否合规并生成验证码，将验证码以 `login:code:{phone}` 的形式存入 Redis，并设置较短的过期时间。

    - 用户输入验证码后发起登录请求，后端从 Redis 校验验证码，如果通过则查询用户是否存在，不存在会自动注册，然后生成一个 token（通常是 UUID）。
    接着将用户信息封装成 UserDTO，转为 Map 存入 Redis 的 Hash 结构中，key 为 `login:token:{token}`，并设置过期时间（如30分钟）。同时将 token 返回给前端。

    - 前端在后续请求中会将 token 放在请求头（authorization）中携带。请求到达后，首先经过第一层拦截器，从请求头中获取 token，并从 Redis 中查询用户信息，如果存在则反序列化为 UserDTO，存入 ThreadLocal 中，同时刷新 token 的过期时间，实现滑动续期。

    - 然后进入第二层拦截器，主要用于鉴权。它会从 ThreadLocal 中获取用户信息，如果是白名单接口则直接放行；否则判断用户是否存在，不存在则返回 401 未登录，存在则放行。

    - 在业务层中，可以通过 ThreadLocal 直接获取当前登录用户信息，用于执行点赞、下单等操作。

    - 请求处理完成后，在拦截器的 afterCompletion 方法中清理 ThreadLocal，防止线程复用导致的用户数据泄漏问题。

2\.为什么使用 Redis 存储登录态？

- **高性能读写**：登录后每次请求都要校验 token，放 Redis 比查 MySQL 快很多，延迟低。

- **天然过期机制**：登录态是短期会话，Redis 可直接设置 TTL，并支持滑动续期（用户活跃就续期）。

- **方便水平扩展**：应用是多实例时，Redis 做集中会话存储，多实例共享同一登录态。

- **减轻数据库压力**：鉴权高频，若每次都打 DB，热点流量下压力很大。

- **支持精细控制**：可按 token 维度做失效、踢下线、风控拦截等，会话管理更灵活。

3\.Redis Hash 是什么？Redis Hash存 Token 有什么优势？

**Redis Hash** 可以理解成 Redis 里的“对象存储结构”，一个 key 下有很多 field\-value，比如：

```Plaintext
login:token:abc123 -> {id: 1, nickName: xxx, icon: xxx}
```

Hash 存 token 会话的优势：

- **对象化存储**：一个 token 对应一个用户对象，字段清晰（id、昵称、头像等）。

- **空间更省**：相比把整段 JSON 字符串直接存 String，Hash 在多字段场景通常更节省内存。

- **天然配合 TTL**：对整个 `login:token:{token}` key 设置过期即可，会话控制简单。

- **业务取值方便**：拦截器里 `opsForHash().entries()` 取出后直接转 `UserDTO`，再放 ThreadLocal。

4\.滑动续期是怎么实现的？为什么要做？

滑动续期在我项目里是放在**请求拦截器**里做的，核心逻辑是：**只要用户在活跃访问，就把 token 过期时间往后延**。

怎么实现（代码链路）：

- 在 `RefreshTokenInterceptor.preHandle()` 里先取请求头 `authorization`。

- 用 token 去 Redis 查 `login:token:{token}`（Hash）。

- 如果能查到用户会话：

    1. 把用户信息写入 `UserHolder(ThreadLocal)`

    2. 调 `expire(login:token:{token}, 30, MINUTES)` 续期

- 如果查不到，就不续期，后续由 `LoginInterceptor` 决定是否拦截。

5\.ThreadLocal 为什么适合存用户信息？

为什么合适：

- **线程隔离**：每个请求线程有自己的副本，不会和其他请求串数据。

- **代码更简洁**：业务层直接 `UserHolder.getUser()`，降低方法签名污染。

在我项目里的用法是：

- `RefreshTokenInterceptor` 查到 token 后 `UserHolder.saveUser(userDTO)`

- 业务里（点赞、关注、下单、签到）通过 `UserHolder.getUser()` 取当前用户

- 请求结束在 `afterCompletion()` 里 `UserHolder.removeUser()` 清理，防线程池复用导致脏数据

6\.ThreadLocal 会不会内存泄漏？为什么？

会，**如果用完不清理就可能内存泄漏**，尤其在线程池场景。

原因我会这样解释：

- ThreadLocal 的值实际挂在线程对象的 `ThreadLocalMap` 上。

- `ThreadLocalMap` 的 key 是弱引用（`WeakReference<ThreadLocal>`），value 是强引用。

- 如果 ThreadLocal key 被 GC 了，但线程还活着（比如 Tomcat 工作线程长期复用），value 可能还残留，直到下次触发清理逻辑，期间就会“泄漏/堆积”。

- 另外更实际的问题是：旧请求用户数据可能残留到线程，下个请求复用该线程时出现“用户串号”。

7\.你们是如何清理 ThreadLocal 的？

我们是在**拦截器统一清理**的

具体做法：

- 请求进入时，在 `preHandle()` 里把用户写入 `UserHolder.saveUser(userDTO)`。

- 请求结束时（无论正常返回还是异常），Spring MVC 会回调 `afterCompletion()`。

- 在这个回调里执行 `UserHolder.removeUser()`，把 ThreadLocal 里的用户上下文移除。

**三、缓存治理**

1\.什么是缓存穿透、缓存击穿、缓存雪崩？

- **缓存穿透**

    - 请求的数据在缓存和数据库里都不存在（例如恶意请求不存在 ID）。

    - 结果：每次都打到 DB，缓存形同虚设。

    - 常见方案：缓存空值、布隆过滤器

- **缓存击穿**

    - 某个“热点 key”在高并发时刚好过期，大量请求同时穿过缓存打 DB。

    - 结果：DB 瞬时压力暴增。

    - 常见方案：互斥锁重建、逻辑过期\+异步重建、热点永不过期。

- **缓存雪崩**

    - 大量 key 在同一时间失效，或 Redis 整体不可用。

    - 结果：大量请求回源 DB，引发级联故障。

    - 常见方案：过期时间加随机值、多级缓存、限流降级、Redis 高可用（主从/哨兵/集群）。

2\.你们为什么选择逻辑过期而不是互斥锁？

选逻辑过期不是因为互斥锁不好，而是因为热点高并发下要先保可用和延迟。逻辑过期允许短暂旧值换取系统稳定，互斥锁更适合强一致但并发相对可控的场景

3\.逻辑过期方案的实现方案和缺点是什么？

核心是“**不过期 Redis key，而是把过期时间写在 value 里**”

实现流程（`setWithLogicalExpire` \+ `queryWithLogicalExpire`）：

- **1）写缓存时**

    - 不直接存业务对象，而是存一个包装对象 `RedisData`：`{ data, expireTime }`。

    - 这里的 `expireTime` 是逻辑过期时间（例如 now \+ 30min）。

- **2）读缓存时**

    - 先读 key，反序列化出 `RedisData`。

    - 如果 `expireTime` 没到：直接返回 `data`。

    - 如果已过期：先返回旧数据（保证可用），同时进行异步重建缓存

- **3）异步重建**

    - 拿到锁的线程提交线程池任务去查 DB、回写新缓存、释放锁。

    - 没拿到锁的线程不阻塞，直接返回旧值，避免并发请求全部卡住。

- **4）并发控制**

    - 通过互斥锁 key（如 `lock:shop:{id}`）保证同一时刻只有一个重建任务，防止缓存重建风暴。

缺点（必须清楚）：

- **短暂不一致**：过期后的一段时间会返回旧数据（最终一致，不是强一致）。

- **实现复杂度更高**：比普通 TTL 缓存多了包装结构、锁、线程池、异常处理。

- **需要兜底机制**：重建线程失败或锁处理不当，可能导致旧数据停留过久。

- **不适合强一致场景**：像余额、库存这类强一致数据不建议用该策略直接兜底读取。

4\.异步重建怎么做的？

异步重建是“**过期后返回旧值 \+ 后台线程重建缓存**”：

- 读缓存发现逻辑过期后，先尝试拿重建锁（避免并发重复重建）。

- 拿到锁的线程把“查 DB \+ 写新缓存”提交到线程池执行（`CacheClient` 里有固定线程池）。

- 没拿到锁的请求不等待，直接返回旧数据，保证接口可用性。

- 重建完成后释放锁，下次请求就能读到新值。

5\.更新 DB \+ 删除缓存为什么能保证一致性？

**“更新 DB \+ 删除缓存”保证的是最终一致，不是强一致**

- 如果“先删缓存再更新 DB”，在两步之间有并发读，请求会把旧 DB 数据重新写回缓存，造成脏缓存。

- “先更新 DB 再删缓存”能把这个风险降很多：删完后下一次读会从新 DB 回填，数据趋于一致。

6\.如果删除缓存失败怎么办？

- **1）异步补偿（推荐）**

    - 仍失败就把“待删除 key”写入消息队列（可用 MQ）。

    - 后台消费者持续重试删除，直到成功。

- **2）延迟双删**

    - 在“更新 DB 后立刻删一次”的基础上，延迟几百毫秒\~几秒再删一次，降低并发读写竞态导致的脏缓存概率。

8\.如何保证热点 Key 不被击穿？

- **第一层：逻辑过期 \+ 异步重建（主策略）**

    - 热点 key 过期时先返回旧值，不让大量请求直接打 DB；

    - 只有拿到锁的少量线程去后台重建缓存。

- **第二层：互斥锁重建（兜底策略）**

    - 对必须回源重建的场景，用分布式锁保证同一时刻只有一个线程查库回填。

    - 避免并发线程同时穿透到 DB。

- **第三层：缓存预热 \+ 不同 TTL 策略**

    - 对已知热点数据提前预热；

    - 给不同 key 设置差异化 TTL，避免同一时刻集中过期。

- **第四层：限流降级**

    - 热点异常时对接口做限流/快速失败，保护数据库。

9\.Redis 挂了怎么办？

**高可用建设**：

- Redis 做主从 \+ Sentinel，或直接 Redis Cluster；

10\.redis作为缓存 Key的结构是怎么设计的

项目的 Redis Key 设计原则是：**业务前缀 \+ 功能域 \+ 业务主键**，并且做到“**一类数据一种结构**”

- **命名规范**

    - 统一小写、冒号分层：`module:biz:id`

    - 例如：`login:code:{phone}`、`login:token:{token}`、`cache:shop:{id}`、`seckill:stock:{voucherId}`

- **按业务域拆分**

    - 登录域：`login:*`

    - 缓存域：`cache:*`

    - 锁域：`lock:*`

    - 秒杀域：`seckill:*`

    - 社交域：`blog:*`、`feed:*`、`follows:*`

    - 地理域：`shop:geo:*`

    - 签到域：`sign:*`

- **按数据结构选型（项目实际）**

    - `String`：验证码、库存

    - `Hash`：登录态用户信息（token \-\> UserDTO字段）

    - `Set`：关注关系、秒杀“一人一单”集合

    - `ZSet`：点赞排行、Feed 收件箱

    - `Bitmap`：签到

    - `GEO`：附近商户

    - `Stream`：异步下单队列（`stream.orders`）

**四、高并发秒杀**

1\.秒杀流程详细讲一下

- **1）请求入口**

    - 用户调用秒杀接口：`VoucherOrderController.seckillVoucher(voucherId)`

    - 进入 `VoucherOrderServiceImpl.seckillVoucher()`。

- **2）生成订单号**

    - 先用 Redis 全局 ID 生成器（`RedisIdWorker`）生成 `orderId`，用于后续链路追踪和落库。

- **3）Lua 原子校验（核心）**

    - 执行

    ```Plaintext
    seckill.lua
    ```

    - ，在 Redis 里一次完成：

        1. 判断库存是否充足（`seckill:stock:{voucherId}`）

        2. 判断用户是否已下单（`seckill:order:{voucherId}` Set）

        3. 扣减库存 \+ 写入用户下单集合

        4. `XADD` 投递消息到 `stream.orders`

    - 脚本返回：`0=成功`、`1=库存不足`、`2=重复下单`。

- **4）接口快速返回**

    - 返回秒杀受理结果，避免请求线程同步阻塞数据库写入。

- **5）异步消费者处理订单**

    - 服务启动后有一个单线程任务持续读取 `stream.orders`（消费者组 `g1`，消费者 `c1`）。

    - 读到消息后转成 `VoucherOrder` 对象进入下单逻辑。

- **6）消费端并发控制与落库**

    - 先按用户维度加 `Redisson` 分布式锁：`lock:order:{userId}`，防同用户并发重复落单。

    - 再执行事务方法

    ```Plaintext
    createVoucherOrder()
    ```

    - ：

        1. 查订单是否已存在（用户 \+ 券）

        2. 条件扣减库存（`stock = stock - 1 and stock > 0`）

        3. 保存订单到 MySQL。

- **7）异常补偿（pending\-list）**

    - 消费异常时会处理 Stream pending\-list，重试未 ACK 的消息，尽量保证最终处理完成。

2\.为什么要用 Lua 脚本？

**多步操作变成 Redis 内的原子操作**，避免并发竞争

- 库存判断、扣减、一人一单是多条命令，分开执行会有并发窗口，可能超卖或重复下单。

3\.Redis 单线程为什么还能支撑高并发？

- **内存操作快**：Redis 数据在内存里，避免磁盘随机 I/O，单次操作本身很快。

- **事件驱动 \+ I/O 多路复用**：一个线程可以高效处理大量连接读写，不是“一次只服务一个客户端”。

- **避免线程切换和锁竞争**：单线程执行命令，天然没有多线程锁争用和上下文切换开销。

- **网络往返可合并**：像我们秒杀用 Lua，一次请求完成多步逻辑，进一步提升吞吐。

4\.一人一单是怎么保证的？

- **第一层：Redis \+ Lua 前置拦截（高并发主防线）**

    - 在 `seckill.lua` 里先查 `seckill:order:{voucherId}` 这个 Set，`SISMEMBER userId` 判断是否下过单；

    - 没下过才允许扣库存并 `SADD userId`；

    - 这一整段和库存校验在同一个 Lua 脚本里，原子执行。

- **第二层：消费端/DB 兜底（最终一致防线）**

    - 异步消费时先加 `Redisson` 用户维度锁（`lock:order:{userId}`）避免同用户并发创建；

    - 事务里再查一次“用户\+券”是否已有订单，存在就拒绝；

    - 并且库存扣减是条件更新（`stock > 0`），防并发超卖。

6\.Redis 扣库存成功但订单落库失败怎么办？

- Lua 成功后会扣 Redis 库存并写入 `stream.orders`；

- 订单由异步消费者落库，消费异常会进入 pending\-list，系统会重读 pending\-list 重试；

- 所以短暂失败一般能通过重试恢复，属于“最终一致”。

7\.MQ 消费失败怎么办？

- **主项目：Redis Stream 消费失败处理**

    - 消费异常时不 ACK，消息会留在 pending\-list；

    - 消费线程会主动扫描 pending\-list 并重试处理；

    - 处理成功后再 ACK，避免消息“丢处理”；

    - 同时业务侧做幂等（按订单号/用户券关系校验），防止重复消费造成脏数据。

9\.如何保证幂等？

- **前置幂等（Redis Lua）**

    - `seckill.lua` 里用 `seckill:order:{voucherId}` Set 判断用户是否下过单；

    - 已下单直接返回，避免大量重复请求进入后续链路。

- **消费端幂等（业务查重）**

    - 异步消费创建订单前，按 `userId + voucherId` 再查 DB；

    - 已存在订单则直接跳过，不重复创建。

- **并发幂等（Redisson 锁）**

    - 消费端按用户维度加锁 `lock:order:{userId}`，避免同一用户并发消费导致重复写。

- **数据库最终硬约束（生产建议）**

    - 建议加唯一索引 `(user_id, voucher_id)`，把幂等从“代码约束”升级到“数据层硬约束”。

10\.为什么要用Redis Stream？和 MQ 有什么区别？

**Redis Stream 是为了轻量异步削峰；MQ（RocketMQ/Kafka）更适合多服务、强治理场景**

为什么这里用 Stream：

- 秒杀请求要快速返回，不能同步落库，必须异步化；

- 项目本身强依赖 Redis，直接用 Stream 接入成本低；

- Stream 支持消费者组、ACK、pending\-list，能满足基础可靠消费。

11. Redis Stream 的 pending list 是什么？

`pending-list` 可以理解为 **“已投递给消费者但还没 ACK 的消息清单”**。

在 Redis Stream 消费组里，消息流程是：

- 消费者用 `XREADGROUP` 读消息后，这条消息会进入该消费组的 pending\-list；

- 只有执行 `XACK` 后，消息才会从 pending\-list 移除；

- 如果消费者宕机或处理异常没 ACK，消息会一直留在 pending\-list，等待后续重试处理。

它解决的问题：

- 防止“读到了但没处理完就丢失”；

- 支持故障恢复和消息补偿（重新拉取 pending 消息继续处理）。

在你这个项目里：

- 秒杀订单消费者处理失败时会去扫 pending\-list，再次处理并 ACK，保证订单消息最终能被消费完成。

12\.如果消费者宕机怎么办？

在这个项目（Redis Stream）里：

- 宕机前已读但未 ACK 的消息会留在 **pending\-list**，不会丢。

- 服务恢复后，消费者会先处理 pending\-list，再继续读新消息。

- 只要业务做了幂等（查重），重复拉到同一消息也不会产生脏数据。

13\.如何保证消息至少消费一次？

在 Redis Stream 里，“至少消费一次”靠的是 **消费组 \+ ACK 机制 \+ pending\-list 重试**。

具体是这样保证的：

- 消费者用 `XREADGROUP` 拉消息后，消息先进入 pending\-list；

- 只有业务处理成功后才 `XACK`；

- 如果处理中异常/宕机没 ACK，消息会一直留在 pending\-list；

- 后续通过扫描 pending\-list 重新消费，直到成功 ACK。

这就是“至少一次”的本质：

- 可能重复，但不会轻易丢。

- 因为可能重复，所以消费者必须幂等（你项目里有查重 \+ 锁兜底）。

14\.Redis Stream 和 RocketMQ 你更推荐哪个？为什么？

- **当前这个项目阶段，我更推荐 Redis Stream**

    - 因为系统是单体、链路主要在站内，且已经深度依赖 Redis；

    - Stream 接入成本低、开发快，足够支撑秒杀异步削峰和至少一次消费。

- **如果系统进入多服务协同，我更推荐 RocketMQ**

    - 多下游消费、消息治理、重试策略、死信、监控可观测性、运维体系都更成熟；

    - 更适合做业务事件总线和跨团队协作。

**五、分布式锁 Redisson**

1\.为什么还要加 Redisson 锁？Lua 不够吗？

**Lua 保证“入队资格正确”，Redisson 保证“落库执行有序”。**

- **Redisson 锁解决消费端并发写库竞争**

    - 订单真正落库是在异步消费者里执行，可能有重复消息、并发消费、重试等情况。

    - 所以在消费端按用户加锁（`lock:order:{userId}`），避免同一用户并发创建订单。

2\.Redisson 锁底层原理是什么？

- **加锁**

    - 底层是 `SET lockKey requestId NX PX ttl`（或等价 Lua），只有 key 不存在才能加锁成功。

    - `requestId` 一般是线程唯一标识，用来标记锁归属。

- **解锁**

    - 不能直接 `DEL`，要先判断“是不是自己加的锁”，再删。

    - Redisson 用 Lua 原子执行“校验 owner \+ 删除”，避免误删他人锁。

- **自动续期（WatchDog）**

    - 如果用默认无固定 leaseTime 的 `lock()`，Redisson 会启动看门狗定时续期，防止业务没执行完锁提前过期。

    - 线程挂掉续期会停止，锁最终超时释放，避免永久死锁。

- **可重入**

    - Redisson 的 `RLock` 支持同线程可重入（底层记录重入计数），更贴近 Java 锁语义。

3\.看门狗机制是什么？

- 调用 `lock()`（不指定 leaseTime）后，Redisson 先给锁一个默认过期时间（通常 30s）。

- 同时启动后台定时任务，在锁还被当前线程持有时，周期性给这个锁续期（例如每 10s 续一次）。

- 只要业务线程活着并持有锁，锁就不会过期被别人抢走。

- 一旦线程挂掉/进程崩溃，续期停止，锁会在超时后自动释放，避免死锁。

4\.Redis 主从切换会不会锁丢失？

会，**有这个风险**。这是 Redis 分布式锁的经典问题。

原因是：

- 你在主节点加锁成功后，如果锁 key 还没同步到从节点；

- 主节点宕机并发生主从切换；

- 新主节点上可能没有这把锁；

- 结果就是“锁看起来丢了”，其他客户端可能再次加锁成功，出现并发执行。

所以我会这样回答面试官：

- **单 Redis 主从架构下，锁不是强一致的**，存在主从切换窗口风险。

- Redisson 本身解决了“加解锁原子性、续期、可重入”，但不能彻底消除主从复制延迟带来的锁丢失。

- 工程上要做双保险：

    1. 业务幂等（重复执行也不出错）；

    2. 数据库硬约束（如用户\+券唯一约束、库存条件更新）。

- 在订单表上加唯一索引：`(user_id, voucher_id)`

- 含义：同一个用户对同一张券，数据库只允许存在 1 条订单。

- 库存条件更新（防超卖）

    - 扣库存 SQL 不是直接 `stock = stock - 1`，而是：
    `update ... set stock = stock - 1 where voucher_id=? and stock > 0`

    - 含义：只有库存大于 0 才允许扣减。

    - 即使多个请求同时来，库存到 0 后其余更新会失败，不会扣成负数。

**六、Redis 数据结构应用**

1\.ZSet 点赞排行榜怎么设计？

- **Key 设计**

    - 每篇博客一个 ZSet：`blog:liked:{blogId}`

- **Member / Score 设计**

    - `member = userId`

    - `score = 点赞时间戳`（`System.currentTimeMillis()`）

    - 这样既能判断用户是否点过赞，也能按时间做 TopN。

- **点赞/取消点赞流程**

    - 点赞：先查 ZSet 是否有该

    ```Plaintext
    userId
    ```

    - ，没有则

        1. DB 的 `liked` 字段 `+1`

        2. `ZADD blog:liked:{blogId} score userId`

    - 取消点赞：有则

        1. DB 的 `liked` 字段 `-1`

        2. `ZREM blog:liked:{blogId} userId`

- **排行榜查询**

    - 用 `ZRANGE blog:liked:{blogId} 0 4` 取前 5 个用户

    - 再按返回顺序去查用户信息并回传（保持排名顺序）

为什么用 ZSet：

- 同时具备“去重”（member 唯一）和“排序”（score）能力；

- 读 TopN 很高效，特别适合点赞榜、热度榜。

2\.GEO 附近商户原理是什么？

GEO 附近商户本质是：**把商户经纬度放到 Redis 的地理索引里，按“点 \+ 半径”做范围检索**

附近商户功能的核心是把商户坐标按类型存入 Redis GEO 结构，比如 `shop:geo:{typeId}`。用户查询时，先以用户当前位置为中心点，在 Redis 中按半径范围检索，并按距离升序返回商户 id 和距离。然后再根据这些 id 回表查询商户详情，并按 Redis 返回的顺序组装结果。

3\.Bitmap 为什么适合签到？

签到本质是“**某天是否签到（0/1）**”

**然按时间位图建模**：

- 第 N 天对应第 N\-1 位，映射直观。

- 签到是典型二值状态场景，Bitmap 用 bit 表示每天是否签到，

4\.Set 关注关系如果数据量 1 亿怎么办？

大量级下，**不能只靠单机 Redis Set 硬扛**，要做分层和分片

### 分层

- **MySQL 存全量关注关系**

- **Redis 只存热点 比如高频访问用户**

### 分片

- **搭建redis集群**

- **不同用户的关注数据分散到不同 Redis 节点**

5\.Redis 内存满了怎么办？

- **先止血（在线应急）**

    - 立即看 `used_memory`、`mem_fragmentation_ratio`、大 key、热点 key。

    - 临时开启/调整淘汰策略（常用 `allkeys-lru` 或 `volatile-lru`）。

    - 对非核心缓存先降级或缩短 TTL，优先保登录态/秒杀关键 key。

- **治理内存结构**

    - 清理无 TTL 或异常长 TTL 的 key；

    - 压缩 value 体积，避免存冗余字段；

    - 处理大 key（拆分、分桶、异步迁移）；

    - 对不适合放 Redis 的数据回迁 DB/ES。

- **架构扩容**

    - 从单机升级到 Redis Cluster 做分片扩容；

    - 读写分离、冷热分层（本地缓存 \+ Redis \+ DB）。

- **策略优化**

    - 不同业务设置分级 TTL（验证码短、会话中、缓存按热度）；

    - 空值缓存设短 TTL；

    - 严格限制“永久 key”。

- **监控预防**

    - 监控内存水位、淘汰数、命中率、bigkey、慢查询；

    - 设阈值告警（比如 70/85/90% 分级告警），提前扩容而不是等打满。

**八、数据库设计**

3\.MySQL 扣库存用什么 SQL？

UPDATE tb\_seckill\_voucher
SET stock = stock \- 1
WHERE voucher\_id = \#\{voucherId\}
AND stock \> 0;

4\.乐观锁还是悲观锁？

这个秒杀场景我选的是**乐观锁思路**（条件更新），不是悲观锁。

- **我实际做法**：`update ... set stock = stock - 1 where voucher_id=? and stock > 0`

- 为什么偏乐观锁

    - 秒杀并发高，悲观锁（`select ... for update`）会导致大量阻塞和锁等待；

    - 乐观方式不阻塞，失败快速返回，更适合高并发抢购。

- 代价

    - 会有更新失败重试/快速失败，但这是可接受的；

- 需要配合幂等和异步链路保证最终一致。

一句话：
秒杀是“高并发、可失败”场景，优先吞吐和快速失败，所以用乐观思路更合适。

**九、项目亮点拷问**

1\.你这个项目你觉得最大的难点是什么？

我觉得最大难点是：**在秒杀高并发下，同时兼顾性能和一致性**。
简单说，就是“既要快，又不能错”。

为什么它最难：

- 入口要扛住瞬时流量，不然 DB 会被打穿；

- 还要保证不超卖、不重复下单、不丢单；

- 一旦引入异步，又会带来消息重试、重复消费、最终一致等问题。

我在项目里的解法是分层闭环：

- **前置层**：`Redis + Lua` 原子做库存校验和一人一单，快速失败削峰；

- **异步层**：`Redis Stream` 解耦请求和落库，平滑 DB 写压力；

- **落库层**：`Redisson` 用户锁 \+ DB 条件更新 \+ 查重兜底，防并发脏写；

- **恢复层**：pending\-list 重试，处理消费异常，保证最终一致。

所以难点不是某个技术点，而是把多种机制串成一条可靠链路。

模拟面试

1\.redis\+lua脚本实现秒杀库存的原子操作，详细说说lua内都包含了哪些逻辑判断，比如一人一单，脚本里怎么实现的？

脚本入参有 3 个：

- `voucherId`：券 ID

- `userId`：用户 ID

- `orderId`：订单 ID（Java 先生成好传进来）

然后脚本内部做了 4 类关键逻辑：

- **1）构造业务 key**

    - 库存 key：`seckill:stock:{voucherId}`

    - 下单用户集合 key：`seckill:order:{voucherId}`

- **2）库存校验**

    - `GET seckill:stock:{voucherId}`

    - 如果 key 不存在或 `stock <= 0`，直接返回 `1`（库存不足）

- **3）一人一单校验**

    - `SISMEMBER seckill:order:{voucherId} {userId}` `SISMEMBER` 命令是用来检查用户是否在已经下单的用户集合中

    - 如果已存在，返回 `2`（重复下单）

- **4）原子扣减 \+ 记录 \+ 入队**

    - `DECR stockKey -1` 扣库存

    - `SADD orderKey userId` 记录该用户已下单

    - `XADD stream.orders ... userId voucherId id(orderId)` 把订单消息写入 Stream

    - 最后返回 `0`（成功）

3\.在异步下单的消费端，你提到用分布式锁redisson按用户维度加锁来避免重复下单，为什么选择用户维度而不是订单或者商品维度？这个锁的粒度怎么考虑的，会不会带来性能问题？

我选**用户维度锁（****`lock:order:{userId}`****）**，因为我要保护的核心约束是：**同一用户同一时刻不能并发创建多笔秒杀订单**。
锁粒度直接对齐业务约束，最自然。

为什么不是订单维度：

- 订单 ID 是下单前生成的，每次请求都不同；

- 用 `orderId` 加锁等于每次都是新锁，完全挡不住“同一用户重复下单”。

为什么不是商品维度：

- 用 `voucherId` 锁会把同一商品所有用户串行化；

- 高并发下吞吐会被严重压低，相当于把热点商品变成单线程处理。

用户维度锁的优点：

- 只串行化“同一用户”的请求，不影响不同用户并发；

- 冲突面小、吞吐高，和“一人一单”约束完全一致。

会不会有性能问题？

- 正常不会成为主要瓶颈，因为大多数用户只抢一次，锁竞争集中在“重复请求用户”；

4\.逻辑过期\+异步重建防止击穿的场景里，具体聊聊逻辑过期怎么实现？你是怎么判断缓存过期了，怎么触发缓存重建的

逻辑过期不是靠 Redis key 的 TTL 来判定，而是把“过期时间”写进缓存值里。

实现分三步：

- **1）写入逻辑过期数据**

    - 缓存里存一个包装对象

    ```Plaintext
    RedisData
    ```

    - `data`：真实业务数据（比如店铺信息）

    - `expireTime`：逻辑过期时间（`now + N 分钟`）

    - 对应方法是 `setWithLogicalExpire(...)`。

- **2）读取并判断是否过期**

    - 先读缓存并反序列化 `RedisData`。

    - 比较

- expireTime

```Plaintext

和当前时间：

- 没过期：直接返回 `data`
- 过期：进入“异步重建”流程
```

- **3）触发异步重建**

    - 过期后先尝试获取互斥锁（防止多个线程同时重建同一个 key）。

    - 拿到锁的线程提交线程池任务：查 DB \-\> 回写新 `RedisData`（新的 `expireTime`）\-\> 释放锁。

    - 没拿到锁的线程不阻塞，直接返回旧值（保证可用性）。

所以“怎么判断过期”：看 `RedisData.expireTime`。
“怎么触发重建”：过期后抢锁，抢到锁的线程异步重建。

5\.更新链路选择了更新DB\+删除缓存来保证一致性，有没有遇到过删除缓存失败，或者删除后立刻有读请求把旧数据塞回缓存的情况？你是怎么应当这种问题的？

1. **延迟双删**：在更新数据库后，延迟一段时间再次删除缓存，避免读请求回填旧数据

1. **异步重试机制**：如果删除缓存失败，可以通过消息队列进行重试，保证最终删除成功

1. **设置缓存过期时间**：为缓存设置合理 TTL，即使出现不一致也能自动恢复

1. **必要时引入分布式锁或版本控制机制**，进一步降低并发问题带来的影响

